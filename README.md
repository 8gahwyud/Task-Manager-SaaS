# TaskFlow — Task Manager SaaS

Минималистичный task-менеджер для команд в стиле Linear/Trello.

**Live Demo:** [https://task-manager-saas.vercel.app](https://task-manager-saas.vercel.app)

---

## 🚀 Возможности

- ✅ Регистрация и авторизация
- ✅ Создание проектов
- ✅ Приглашение участников по email
- ✅ Kanban-доска с drag-and-drop
- ✅ Задачи с приоритетами и дедлайнами
- ✅ Аналитика по проектам

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND                              │
│                     (Next.js 14 + React)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Страницы  │  │ Компоненты  │  │    Состояние        │  │
│  │  /login     │  │  KanbanBoard│  │  React State +      │  │
│  │  /register  │  │  TaskCard   │  │  Server Components  │  │
│  │  /dashboard │  │  Modal      │  │                     │  │
│  │  /projects  │  │  Sidebar    │  │                     │  │
│  │  /analytics │  │             │  │                     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         BACKEND                              │
│                    (Next.js API Routes)                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    API Endpoints                     │    │
│  │  POST /api/auth/register     - Регистрация          │    │
│  │  POST /api/auth/[...nextauth]- Вход/Выход           │    │
│  │  GET  /api/projects          - Список проектов      │    │
│  │  POST /api/projects          - Создать проект       │    │
│  │  GET  /api/projects/[id]     - Получить проект      │    │
│  │  POST /api/projects/[id]/invite - Пригласить        │    │
│  │  POST /api/tasks             - Создать задачу       │    │
│  │  PATCH/api/tasks/[id]        - Обновить задачу      │    │
│  │  DELETE/api/tasks/[id]       - Удалить задачу       │    │
│  └─────────────────────────────────────────────────────┘    │
│                              │                               │
│                              ▼                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   Prisma ORM                         │    │
│  │           (Типизированные запросы к БД)              │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        DATABASE                              │
│                  (PostgreSQL на Neon)                        │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │   User    │ │  Project  │ │   Task    │ │ Invitation│   │
│  │ ───────── │ │ ───────── │ │ ───────── │ │ ───────── │   │
│  │ id        │ │ id        │ │ id        │ │ id        │   │
│  │ email     │ │ name      │ │ title     │ │ email     │   │
│  │ password  │ │ ownerId   │ │ status    │ │ projectId │   │
│  │ name      │ │           │ │ priority  │ │ token     │   │
│  └───────────┘ └───────────┘ │ deadline  │ └───────────┘   │
│        │              │      │ assigneeId│                  │
│        └──────────────┼──────┴───────────┘                  │
│               ProjectMember                                  │
│              (связь many-to-many)                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
taskflow/
├── prisma/
│   └── schema.prisma      # Схема базы данных
├── src/
│   ├── app/               # Next.js App Router
│   │   ├── (auth)/        # Публичные страницы (login, register)
│   │   ├── (dashboard)/   # Защищённые страницы
│   │   │   ├── dashboard/     # Главная панель
│   │   │   ├── projects/      # Проекты и Kanban
│   │   │   └── analytics/     # Аналитика
│   │   ├── api/           # Backend API
│   │   │   ├── auth/          # Аутентификация
│   │   │   ├── projects/      # CRUD проектов
│   │   │   └── tasks/         # CRUD задач
│   │   ├── layout.tsx     # Корневой layout
│   │   └── page.tsx       # Landing page
│   ├── components/        # React компоненты
│   │   ├── KanbanBoard.tsx    # Доска с drag-and-drop
│   │   ├── TaskCard.tsx       # Карточка задачи
│   │   ├── Modal.tsx          # Модальные окна
│   │   └── Sidebar.tsx        # Боковое меню
│   ├── lib/               # Утилиты
│   │   ├── auth.ts            # Конфиг NextAuth
│   │   └── prisma.ts          # Клиент Prisma
│   └── types/             # TypeScript типы
└── package.json
```

---

## 🔧 Как работает Backend

### Аутентификация (NextAuth.js)

```typescript
// src/lib/auth.ts
// Используем Credentials Provider с bcrypt для хеширования паролей

// Регистрация: POST /api/auth/register
// 1. Валидация данных через Zod
// 2. Проверка уникальности email
// 3. Хеширование пароля (bcrypt)
// 4. Создание пользователя в БД

// Вход: через NextAuth
// 1. Поиск пользователя по email
// 2. Сравнение паролей (bcrypt.compare)
// 3. Создание JWT сессии
```

### API роуты

```typescript
// Все API используют один паттерн:

export async function GET/POST/PATCH/DELETE(request: Request) {
  // 1. Проверка авторизации
  const session = await getServerSession(authOptions)
  if (!session) return 401

  // 2. Валидация входных данных (Zod)
  const data = schema.parse(await request.json())

  // 3. Проверка прав доступа
  const hasAccess = await checkAccess(session.user.id, resourceId)

  // 4. Выполнение операции через Prisma
  const result = await prisma.model.create/update/delete(...)

  // 5. Возврат результата
  return NextResponse.json(result)
}
```

### Prisma ORM

```typescript
// src/lib/prisma.ts
// Singleton паттерн для переиспользования подключения

// Примеры запросов:

// Получить проекты пользователя
prisma.project.findMany({
  where: {
    OR: [
      { ownerId: userId },
      { members: { some: { userId } } }
    ]
  },
  include: { tasks: true, members: true }
})

// Обновить задачу
prisma.task.update({
  where: { id: taskId },
  data: { status: 'done', position: newPosition }
})
```

---

## 🎨 Как работает Frontend

### Защита роутов

```typescript
// src/app/(dashboard)/layout.tsx
// Server Component проверяет сессию
// Если не авторизован → redirect('/login')
```

### Kanban Drag & Drop

```typescript
// src/components/KanbanBoard.tsx
// Использует @dnd-kit/core

// 1. DndContext оборачивает колонки
// 2. При перетаскивании:
//    - onDragStart: сохраняем активную задачу
//    - onDragOver: определяем новый статус
//    - onDragEnd: отправляем PATCH запрос на сервер
// 3. Оптимистичное обновление UI
```

### Состояние

```typescript
// Используем комбинацию:
// - Server Components для начальных данных
// - useState для локального состояния (модалки, формы)
// - Оптимистичные обновления при drag-and-drop
```

---

## 🗄️ Модели базы данных

### User (Пользователь)
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | Уникальный ID |
| email | TEXT | Email (уникальный) |
| password | TEXT | Хеш пароля (bcrypt) |
| name | TEXT | Имя пользователя |

### Project (Проект)
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | Уникальный ID |
| name | TEXT | Название |
| description | TEXT? | Описание |
| ownerId | TEXT | ID владельца |

### Task (Задача)
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | Уникальный ID |
| title | TEXT | Название |
| description | TEXT? | Описание |
| status | TEXT | todo / in_progress / review / done |
| priority | TEXT | low / medium / high / urgent |
| deadline | TIMESTAMP? | Дедлайн |
| position | INT | Позиция в колонке |
| projectId | TEXT | ID проекта |
| assigneeId | TEXT? | ID исполнителя |
| creatorId | TEXT | ID создателя |

### ProjectMember (Участник проекта)
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | Уникальный ID |
| role | TEXT | owner / admin / member |
| userId | TEXT | ID пользователя |
| projectId | TEXT | ID проекта |

---

## 🛠️ Технологии

| Слой | Технология | Зачем |
|------|------------|-------|
| Frontend | Next.js 14 | React фреймворк с SSR |
| Стили | Tailwind CSS | Utility-first CSS |
| Drag & Drop | @dnd-kit | Современный DnD для React |
| Графики | Recharts | Визуализация данных |
| Анимации | Framer Motion | Плавные переходы |
| Backend | Next.js API Routes | Serverless API |
| Auth | NextAuth.js | Аутентификация |
| ORM | Prisma | Типизированные запросы к БД |
| Database | PostgreSQL (Neon) | Облачная БД |
| Хостинг | Vercel | Деплой и CDN |

---

## 🚀 Локальная разработка

```bash
# Клонировать
git clone https://github.com/8gahwyud/Task-Manager-SaaS.git
cd Task-Manager-SaaS

# Установить зависимости
npm install

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env

# Применить схему к БД
npx prisma db push

# Запустить
npm run dev
```

---

## 📝 Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| DATABASE_URL | PostgreSQL connection string | postgresql://user:pass@host/db |
| NEXTAUTH_SECRET | Секрет для JWT | случайная строка 32+ символа |
| NEXTAUTH_URL | URL приложения | https://your-app.vercel.app |

---

## 📄 Лицензия

MIT

---

**Создано за выходные** ✨
